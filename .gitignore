# Hugo build output
/public/
/resources/_gen/
.hugo_build.lock

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Node (only if you add JS tooling)
node_modules/

# Claude Code local config — settings, the dev-server launch entries and the
# writing guide. Working notes, not site content, so none of it is pushed.
.claude/

# Wrangler local state (contains account info)
.wrangler/

# Working assets, not part of the site
/reference assets/

# Local writing guide, not part of the site (.claude/voice.md is covered above)
CLAUDE.md
